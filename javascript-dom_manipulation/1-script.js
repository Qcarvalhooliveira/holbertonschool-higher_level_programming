let headerElement = document.querySelector('header'); // select the header element


red_header.addEventListener('click', () => {
    headerElement.style.color = '#FF0000';
    }); // added event listener to the red_header id to change the color of the header element on click